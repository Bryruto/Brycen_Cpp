Brycen's c++ doc 

git stuff 
    git init -give you a path to up and for the module you are in i think
    git add . -stages the changes you made 
    git commit -am "word" -gives everything ready to push 
    git push -update the repo with changes 
    git submudule add link -this will let you add other dispo to your dispo 

how to run test
    g++ cd/file.cpp test/test.a test/test.o -lpthread -o name
    compile all files you want to compile and link -lpthread for google test threads -o means name then name it anything
    
cl name.cpp to compile cl *.cpp to compile all 
.\name.exe to run 
brycen's docs
    cin cout
     namespace
        std::cout << varint << std::endl;
        // std -> is a namespace for iostream library
        // :: -> means to go into
        // cout -> is a object in the name space for displaying stuff to a screen
        // << -> this is a overloaded function or insertion operater 
        // endl -> is a function called a stream manipulator it will flush the buffer and move to a new line
                flushing the buffer means write to screen right away
        varint= 20;

        std::cin.get();
        //cin -> object to ask for input from keyboard
    1 preprocessor statments / compiler

        when running a c++ program
            source code
                ↓
            preprocessor
                ↓
            compiler
                ↓
            assembler
                ↓
            linker
                ↓
            executable

        preprocessor statements

            all preprocessor statements start with '#'
                <> - only use standard / system libraries system include paths only
                "" - current file directory first then system include paths you can use this every time if 
                    you like

                #include 
                    
                    syntax = #include "file.h" or #include <library>
                    all this means is to copy what is in the library and paste it in file 
                #define 

                    syntax = #define replace_this with_this
                    
                    #define INTERGER people
                    now to make an int its not int its people 
                    people some_people = 7; will make a int

                    you cant use int until you take away the #define away
                #if
                    works like a conditional 
                    #ifndef any_name - with not define already
                        if not defined it what it means 
                    #ifdef any_name
                        if defined
                    //anything under the these will be apart of the define behaivor 

                    #endif - this will say you are done with if statement 

        compiler:
            the main job of the compiler is to convert all code into constent data or instuctions
            this will make the real machine code the computer runs 

            after you compile a cpp file you will get translation unit that are then converted to a object
            obj file and and executable exe file
    2 variable

        variable allow you to name a piece of data you store in memory so that you can
        the only real difference in variables is how big they are becuase c++ is weird

        basic syntax
            type name; this will make a varaiable that you can use in your code later
            type name = value; this will make and set a value at that memory location
            name = value;this is how you update an already made value 

            //in c++ you can do this stuff in a lot of way but this is the main way

        primitive data types

            8 bits -> 1 byte

            ints
                unsigned -> to only get positive 

                int -> 4 bytes  //-2b to 2b  
                short -> 2 byte
                long -> 4 bytes // 8 on mac and linux
                long long -> 8 bytes

                ex
                    declaration 
                        int a;
                        short b;
                        long c;
                        long long d;

                    initialization:
                        int variable = 5;
                        short variable = 1;
                        long variable = 3;
                        long long variable = 78;

            float
                float  -> 4 bytes
                double -> 8 bytes
                long double -> 8 - 16 bytes defined by compiler

                ex
                    declaraition:
                        float b;
                        double c;
                        long double t;

                    initialization:
                        float variable = 5.5f; -> you have to add the f or its a double
                        double var = 5.2; 

            char 
                this will go off of ascii values so 65 is A and so on

                char -> 1 byte Ascii
                char16_t -> 2 bytes UTF-16
                char32_t -> 4 bytes UTF-32
                wchar_t -> Unicode

            bool
                true or false 

                bool -> 1 byte
                
                ex 
                    bool isok = true;

                operators 
                    == this is asking if the value are equal so it compares every bit from each 
                    < this is less than 
                    > this is greater than
                    <= this is less than or equal
                    >= this is greater than or equal
                    !,not this not so not true is false not false is true you can use the sign or the word not
                    or,|| this is or so only 1 needs to be true for it to be true you can use the sign or the word or
                    and,&& this is an and so both need to be true for a true sign or word and 

            void
    
                void just means nothing this is made so there is no trash values  
        
        sizeof / size_t:
            need #include <cstddef> to use size_t not really but you should have it if you do 
            size_t - use for counting and size of stuff works better for all archatecters 

                An unsigned integer type capable of representing the size of the largest possible object. 
                is an int used to represent sizes and counts it can represent all vlid sizes

                this is better becuase its what the system will convert your int value anyways after sizeof
                
                std::size_t n = 42;
            sizeof
                the size of function will convert your varaiable and give you the max space it may take 
                up as a size_t varaiable which is just an unsigned int that is made to work better for 
                memory stuff 
        
        rounding float-double 
            just for printing 
                #include <iostream>
                #include <iomanip>

                std::cout << std::fixed << std::setprecision(decimal place round to) << variable << endl;

            change value
                #include <cmath>
                
                double number = 3.14159;<- number we are rounding to decimal place
                double decimal = std::pow(10.0,int decimal place);<- 10^decimal_place 
                std::round(number*decimal)/decimal;<- add decimal back

                so this is like a shift the dicimal point to the right with * then move to the left with / 
            
        strings
            #include <string>
            using std::string; // dont have to say std every time

            for (char c : string)      // read characters
            for (char& c : string)    // modify characters <- use this one

            creating strings
                string s5{"word"}          // best 
                string s1;                 // empty
                string s2 = "hello";       // from literal
                string s3("world");        // constructor - dont use
                string s4 = s2;            // copy
                string s5(s3.rbegin(),s3.rend()) // you can copy with this and reverse a string with this begin(), end() just add an r to reverse
            string functions:
                size and length                   
                    s.size();                  // number of characters
                    s.length();                // same as size()
                    s.empty();                 // true if size == 0 
                    s.clear();                 // erase contens
                indexing locations:
                    s[0];                      //fast, no bounds check you will get errors
                    s.at(0);                   //no errors just will not work if nothing there
                    s.front();                 //first char
                    s.back();                  //last char
                adding:
                    s+="!";                    //add to the end +=modify existing string + adds a new string
                    s.append("world");         //add a substring to the end same as += 
                    s.insert(index,string);    //adds at that index
                    s.pushback('x');           //only add 1 char at a time
                removing:
                    s.pop_back();              //remove the last char
                    s.earse(start,count=end)   //remove index for start and count is just that many over
                    s.clear();                 //empty string
                    s.replace(start,count,replacement); //from start count after start replace
                find char:
                    s.find(value);             //find first char with the value
                    s.rfind(value);            //find but reversed
                substring use a lot
                    s.substr(start,count);     //start then count over between will give you substring
                conversion:
                    std::stroi(string);         // string -> int
                    std::to_string(int);       // int -> string
                    std::tranform(first,last,out_first,unary_op)            // need <algorithm>
                user input

                    std::getline(std::cin, string_variable);  // read whole line <- best

                    char c = std::cin.get();  // read ONE raw character

                    std::cout << "Enter value: ";
                    std::cin >> variable;
                    // this is getting input but only 1 word so no spaces

                    std::cin  >>  value;      // token input
                    getline(cin, string);     // line input
    3 functions
        functions are used to increase readablity and decrease redundant code 

        basic syntax
    
            return_type name(type name){actions}

                ex
                    int add(int a,int b){
                        return a+b;
                    }

            all functions must have a return except void 

        ex

            example 1 basic

                int Multiply(int a,int b){
                    return a * b;
                }

                int main(){
                    int result = Multiply(3,2)
                    std::cout << result << std::endl;
                }

            example 2 many functions
                
                int Multiply(int a,int b){
                    return a * b;
                }

                void MultiplyandLog(int a,int b){
                    int result = a * b
                    std::cout << result << std::endl;
                }

                int main(){
                    MultiplyandLog(3,2)
                    MultiplyandLog(5,8)
                    MultiplyandLog(90,45)
                }
    4 headers 

        headers file holds your function declarations so you can use in your cpp files
        if you want to use them in all your files you would have to copy the declarations to each 
        one but with header files you just use the preprocessor #include "file.h" and it will copy 
        that files context into the file so you only have to write one line and not more 

        syntax 

            declarations
                type function(arguments);->this is all

            definitions
                type function(arguments){
                    actions
                }

        declarations
            declarations will just say to the computer that there is a function with this name
            that takes these varaible and returns this type else where in your code 
  
        ex
            example 1
                #Log.h <-this is a file

                    #pragma once<- this is a header guard preventes us from inculeding a file 
                    //multiple times in a single translation unit so stops 
                        //other way to do pragma
                            #ifndef _LOG_H
                            #define _LOG_H

                            void InitLog();
                            void Log(const char* message);

                            #endif

                    #include <iostream>

                    void InitLog();
                    void Log(const char* message);
                
                #Log.cpp <-this is a file

                    #include "Log.h"

                    void InitLog(){
                        Log("Initializing Log");
                    }

                    void Log(const char* message){
                        std::cout << message << std::endl;
                    }

                #main.cpp <-this is a file
                    #include "Log.h"

                    int main(){
                        InitLog();
                        Log("Hello World");
                    }
    5 conditionals
        
        ternary operator
            (condition) ? if true : if false;

            ex
                std::cout << "The max is: " << ((a > b) ? a : b) << std::endl;

                int x = (a > b) ? a : b; 
                
                return (condition) ? value1 : value2;

        the move if statements the worse code 
        a conditionals take a bool value which is true or false

        operators 
            == this is asking if the value are equal so it compares every bit from each 
            < this is less than 
            > this is greater than
            <= this is less than or equal
            >= this is greater than or equal
            !,not this not so not true is false not false is true you can use the sign or the word not
            or,|| this is or so only 1 needs to be true for it to be true you can use the sign or the word or
            and,&& this is an and so both need to be true for a true sign or word and 

        basic syntax    
            single if- if (boolion expression){actions}


            if(boolion expression){actions}
            if else (boolin expression){action}
            else{action}



            explain:
                if(varaible) // is just asking is this variable any thing but 0 if so run
                                // this also works for pointers


                how else if works:
                    //it runs a else in that else is an if then if not true it goes to the next 

                
                    else{
                        if{

                        }
                    }

                    //is the same as ubove is what the compiler is really doing

                    else if {

                    }

        ex:
            example 1 basic:

                int main()
                {   
                    int x = 5;
                    bool comparisonResult = x == 5;
                    if (comparisonResult){ //same as if(1)
                        std::cout << "Hello, World" << std::endl;
                    }
                    std::cin.get();
                }
            
            example 2 simp of 1:

                int main()
                {   
                    int x = 5;
                    if (x == 5) 
                        std::cout << "Hello, World" << std::endl;
                    std::cin.get();
                }

            example 3 simp of 2:

                int main()
                {
                    int x = 5;
                    if(x == 5) std::cout<<"Hello,World" << std::endl;
                    std::cin.get();
                }

            example 4 if-chain:

                void Log(const char* word){
                    std::cout << word << std::endl;
                }

                int main(){
                        const char* ptr = "Hello";
                        if (ptr){
                            Log(ptr);
                        }
                        else if (ptr == "Hello"){
                            Log("Ptr is Hello!");
                        }
                        else{
                            Log("Ptr is null!");
                        }
                        std::cin.get();
                    }

            example 5 simp of 4:

                        example 4 if-chain:

                void Log(const char* word){
                    std::cout << word << std::endl;
                }

                int main(){
                        const char* ptr = "Hello";
                        if (ptr)
                            Log(ptr);
                        else if (ptr == "Hello")
                            Log("Ptr is Hello!");
                        else
                            Log("Ptr is null!");

                        std::cin.get();
                    }

                //if it is just one line you dont need to put {} only do that if its more than 1 line
                // you can all so put them on the same line like this 

                    if (ptr) Log(ptr);
                    else if (ptr == "Hello") Log("Ptr is Hello!");
                    else Log("Ptr is null!");
   
            example 6 initialize in if statemen
                int num{7};
                
                if (int v{2*num};v > 10){
                    return v;
                }
                else{
                    return num;
                }

                this is using braced initialization variable v and make of type int 
                then check if the condition is statisfied 
    6 loops:

        use for loop when you know then number of times you want to interate 
        use while loop if you dont know how long 
        use do while if you want at least 1 time

        for loop:
            
            all parts are optional if you dont put anything its a infinite loop 

            rules 
            -all variables must be the same type
            -exactly two ;
            -key word for and ()
            
            ya thats all look at example 2 under ex to see what i mean you can do what ever you want 

            in the update you must re-initializes the value if anything greater then 1 
            short hand 
                i++ this is incroment up by 1 
                i-- decroment by 1 
                i+=2 is add 2
                i*=3 is times 3

            syntax:
                basic for loop
                    for(initialization;condition;update){
                        actions to do each time
                    }
                
                infinite for loop:
                    for(;;){
                        actions
                    }

                many varaiable for loop:
                    for (int a = 10 ,int b = 5;a>b;b+=2,a++){
                        actions
                    }

                range based for loop:
                    for (type variable : range){<- works like python for i in array
                        actions
                     }
                        : -> this is in but in c++ 
                    ex 
                        int{0};
                        for (int num : nums){
                            total += num;
                        }
                        this will add the total of all the elements in the array
           
            ex:
                example 1 basic for loop:

                    void Log(const char* word){
                        std::cout << word << std::endl;
                    }

                    int main(){
                         for (int i = 0; i < 5; i++){
                            Log("Hello World");
                        }
                    }
                
                example 2 weird but works:

                    int i = 0;
                    bool condition = true;
                    for ( ;condition; ){
                        std::cout << i << std::endl;
                        i++;
                        if (!(i<5)) 
                            condition = false;
                    }

                    //this is to show you can do what every you want just small rules to follow
        while loop:

            good for like game loops where you keep track of the state of the game if running

            syntax:

                basic:
                    while (condition){
                        action
                    }

            ex:
                example 1:

                    int i = 0;
                    while (i<5){
                        i++;
                    }
        do while loop:

            it will run at least 1 time then it checks the condition 

            syntax:

                do {
                    action
                    }while (condition)

        code i have learned form:
            example 1:
                #include <string>

                std::string DNAtoRNA(std::string dna){ 
                for(char& c : dna) { -> get a reference for each char thats why & this lets me change the 
                                        values in the dna itself
                    if (c == 'T'){
                        c = 'U';
                    }
                }
                return dna;
                } 
    7 control flow 
        continue;
            so for a continue key word like it means you are done with this itteration 
            do the next itteration 

                example:
                    int main(){
                        for(size_t i{0};i <= 5;i++){
                            if(i != 5)
                                continue; <- end this itteration here and update values 
                            std::cout << "its 5 now" << std::endl;
                        }
                    }
                becuase of continue the message will only print 1 time at continue the itteration i
                is told to just update and go on to the next itteration 
        break;
            this is used for leaving the inner loop when ever you want 

            example 
                int main(){
                    while (true){<- this loop  will run forever 
                        break; <- this makes it leave the forever loop and run the rest of the code 
                    }
                }
        return
            this is a function to give a new value back to the location that called the function 
            must match the same type as in the function

            example:
                int main(){<- int is the return type of main
                    return 0; <-this will send the value 0 back to place that called function 
                                also nothing after this will run 

                    std::cout << "Hello World" << std::endl;<-this line is known as dead code and it will
                                                                never run
                }

                int x = main()<-this is a call and after return x is equal to 0
    8 Array / vector - dynamic array

        find the size of the array best:
            best solution in modern c++:
                #include <iterator>
                #include <cstddef> 
                
                size_t n_elements = std::size(array);
        removing from a vector
            std::erase(v,value_to_removev); // this is all values

            v.erase(value_to_removev); // this is 1 value 

            for raw array:
                #include <cstddef> 
                size_t n_elements = sizeof(array) / sizeof(elements_type)

                std::vector<type> = {values,values,...};

                ex:
                    short array[] =  { 104, 105, 32, 98, 105, 108, 108, 0 };
                    size_t n_elements = sizeof(array) / sizeof(array[0]);
    9 enums / PODs
        enums

            enum is short enumeration enum is just a integer
            which is just a set of values where each name has an int 
            a -> 1 
            b -> 2 
            c -> 3

            example
                this is bad and what enum can fix
                    #include <iostream>

                    int A = 0;
                    int B = 1;
                    int C = 2;

                    int main(){
                        int value = B;
                        
                        if (value == b){do somthing}
                    }
                    why is this bad a,b,c are not together at all 

                with an enum 
                    #include <iostream>

                    enum Example : char<- dont need this : part but it lets you set the type must be int char is int 
                    {
                        A,B,C <- 0,1,2 are the values by default <-A=1,B=2,C=3 would work to set the values
                    };  


                    int main(){
                        Example value = B;<-type enum it must be A,B,or C 
                        
                        if (value == B){<-same as value == 1 B is just an int
                            //do somthing
                            }
                    }
    10 classes/struct:
        object-oriented languages
            really just a why of programing 
            it makes it so you can make your own data types and manipulate it they why you want
        
        c++ does not force a style on you any you can do with a class you can do without 
         
        satic in a class 
            means there is one one variable for all instances of the class
            meaning if one of the instances changes that variable it happens over all instances
            by doing this you can use it like a global variable so if use instance in main it will be the 
            same variable no matter every time no matter the instance 

        classes 
            private by default

            constructors
                a constructors is a method that automaticly runs every time we instantiate/make new instance of a class
                does not have a return type and must be the same name as the class

                if you dont write a constructer you get the default constructer which is made for you but that one is empty

                in cpp you must initialize your primitive data types 

                you can make as many as you like ofc with a different amount of arguments
                
                #include <iostream>
                example
                    example 1 constructers 
                        class Entity
                        {
                            public:
                                float x,y;

                            Entity(){
                                x = 0.0f;
                                y = 0.0f;
                            }

                            Entity(float i, float i2){
                                x = i; 
                                y = i2; 
                            }

                            void Print()
                            {
                                std::cout << x << ", " << y << std::endl;
                            }
                        }; 

                        int main() 
                        {
                            Entity e;<-calls constructer with no arguments
                            Entity e(10.0f,5.0f);<- calls constructer with 2 arguments
                            e.Print();
                        }
                    example 2 
                    class Log{
                        private:
                            Log() {} <- one way 

                        public:
                            Log() = delete;<-second way

                            static void Write(){}
                    }

                    int main(){
                        Log::Write();
                        Log l;<- stop people from doing this
                    }
                    
                    notes
                        these are how to get around having a default constructor
                        first way makes the constructer private so the default does not get made and instance cant get to private
                        
                        second way just delete the constructer cant use what you dont have   

            destructors 
                a method that runs when you destory an object 
                you know something is a destructer when it has a ~ 

                if you use stuff on the heap and need to free that manualy 
                example
                    example 1 
                        class Entity{
                            public:
                                float X, Y;

                                Entity(float x,float y)
                                {
                                    X = x;
                                    Y = y; 
                                }

                                ~Entity()<-this happens when you level the scope of where this was made
                                {
                                    std::cout << "Destroyed" << std::endl;
                                }
                        };

            initialization of an instance 
                Person <- class name 
                brycen <- variable name 

                Person brycen; -> this is default no member variable set yet

                Person brycen ("Brycen",20) -> direct initialization: calls constructor with arguments 

                Person brycen = Person("Brycen", 20); -> this makes a copy of the object 

                Person brycen{"Brycen",20}; -> direct-list initialization -- best practice 

                Person brycen = {"Brycen",20}; -> copy-list initialization 

            methods
                they are functions that are in a class 

                what a class method really looks like 
                    stactic void Print(class instance)
                    {
                        std::cout << intatnce.x << ", " << intatnce.y << std::endl; 
                    }
            
            Inheritance
                allows us to have a hierarchy of classes that are linked parent class and child this is good so we dont have 
                duplicating code 

                allows you to exstend an existing class and add functionality to it

                example
                    example 1 
                        class Entity--super class
                        {
                            public: 
                                float X,Y;

                                void Move(float xa, float ya){
                                    X += xa; 
                                    Y += ya;
                                }
                        };

                        --sub class
                        class Player : public Entity <-child class : parent class if in public of entity 
                        {               --anything that is not private to Entity can be used by player 
                            public:
                                const char* Name;
                                
                                void PrintName(){
                                    std::cout << Name << std::endl;
                                }
                        };

                        int main(){
                            std::cout << sizeof(Entity) << std::endl; <- 8 bytes 
                            std::cout << sizeof(Player) << std::endl; <- 12 bytes
                            Player player;
                            player.Move(5,5);
                            player.X = 2;
                        }

            polymorphism
                you can have many types for a single type
            
            virtual functions
                allow us to overiding methods in different classes

                #include <iostream>
                #include <string>

                class Entity
                {
                    public:
                        std::string GetName() {return "Entity";}
                };

                class Player : public Entity 
                {
                    private:
                        std::string m_Name;
                    public:
                        Player(const std::string& name) 
                            : m_Name(name) {} <-this is calling the constructor for the string class and setting that 
                                                member variable to name that is passed in
                                                this is all done in one step and considered a lot cleaner 
                                                this is called initializer list so constructor call then constructor call again
                                                for the member variable

                        std::string GetName() { return m_Name;}
                }

                int main(){
                    Entity

                    std::cin.get();
                }

                notes
                    if you us initializer list then it will call in the order you have in you class variable not in the 
                    order you use initializer list 
        structs
            public by default

            use when you are taking about POD 
            so if you just want variable to be connected and accessable 

            example
                struct Vec2{
                    float i;
                    float k;
                }

                but you can still add methods

        examples
            example 1 basic method visibility and refernences 
                class Player{
                    public:<-to make it so you can access anywhere 
                        int x,y; 
                        int speed;

                    methods are functions inside of a class and or struct
                        void move(int xa, int ya)<-you dont need a reference to the player anymore because you are in the class
                        {
                            x += xa * speed; <-x,y,and speed are in this scope 
                            y += ya * speed;
                        }
                    }; 
                
                    say we want to more the player instance you can do this
                        void Move(Player& player,int xa,int ya){<- you have to pass a reference to the player 
                                                                    lets you change the value in the class otherwise 
                                                                    you are just using a copy
                            player.x += xa * player.speed;<-to access anything in the class you use dot notation
                            player.y += ya * player.speed; 

                        }
                    
                    int main(){
                        Player player; <-that is how you make an instance of a class <-instantiation
                                        you can call this an instance or object of a classes
                        player.x 5;  
                        move(player,1,-1); <- this is calling a function
                        player.move(1,-1); <- this is using calling a method 

                    }

            example 2 static for a class
                #include <iostream>
                
                ---------basic class before static 
                    struct Entity
                        {
                        int x, y;<- member variables  

                        void Print(){
                            std::cout << x << ", " << y << std::endl;
                        }
                        };
                    
                    int main {
                        Entity e; <- instance of the class 
                        e.x = 2;
                        e.y = 3; <-setting the variable members to values

                        Entity e1 = {5,8}; <- instace of class and set values

                        e.Print();
                        e1.Print();
                        } 
                        
                        output 
                        2,3
                        5,8

                ---------using static class variables and methods

                    struct Entity
                    {
                        static int x, y;<- member variables  

                        static void Print(){<- static method 
                            std::cout << x << ", " << y << std::endl;<- x,y must be static for this to work
                            }
                    };
                    
                    int Entity::x;<-makes x,y static class variables global
                    int Entity::y;

                    int main {
                        Entity::x = 2; <- same behaivor if this is the same as above 
                        Entity::y = 3;

                        Entity::x = 5; 
                        Entity::y = 8; 

                        Entity::Print();
                        Entity::Print();
                    }

                    output
                    5,8
                    5,8

                    why i do it like this
                        as you can see i call the Entity class that is because if a class variable is staic it is the same over all 
                        instances of that class 
                        
                        that is also why i got rid of the instances of the class both e1 and e had the same x,y 

                    why do it like this 

            example 3 log class with enum
                #include <iostream>
                no enum
                    class Log{
                        public:
                            const int LogLevelError = 0;<-const cant change will cause error
                            const int LogLevelWarning = 1; 
                            const int LogLevelInfo = 2;

                        private: 
                            int m_ogLevel = LogLevelInfo;

                        public:
                            void SetLevel(int level)
                            {
                                m_LogLevel = level;
                            } 

                            void Error(const char* message)
                            {
                                if (m_LofLevel >= LogLevelError)
                                    std::cout << "[ERROR]: " << message << std::endl;
                            }

                            void Warn(const char* message)
                            {
                                if (m_LofLevel >= LogLevelWarning)
                                    std::cout << "[ERROR]: " << message << std::endl;
                            }

                            void Info(const char* message)
                            {
                                if (m_LofLevel >= LogLevelInfo)
                                    std::cout << "[ERROR]: " << message << std::endl;
                            }

                    };

                    int main()
                    {
                        Log log; 
                        log.SetLevel(log.LogLevelError);
                        log.Warn("Hello!");
                        log.Error("Hello!");
                        log.Info("Hello!");
                    }
                ----------------with enum
                    class Log{
                        public:
                            enum Level{
                                LevelError,LevelWarning,LevelInfo
                            };
                        private: 
                            Level m_LogLevel = Info;

                        public:
                            void SetLevel(Level level)
                            {
                                m_LogLevel = level;
                            } 

                            void Error(const char* message)
                            {
                                if (m_LofLevel >= LevelError)
                                    std::cout << "[ERROR]: " << message << std::endl;
                            }

                            void Warn(const char* message)
                            {
                                if (m_LofLevel >= LevelWarning)
                                    std::cout << "[ERROR]: " << message << std::endl;
                            }

                            void Info(const char* message)
                            {
                                if (m_LofLevel >= LevelInfo)
                                    std::cout << "[ERROR]: " << message << std::endl;
                            }

                    };

                    int main()
                    {
                        Log log; 
                        log.SetLevel(log::LevelError);
                        log.Warn("Hello!");
                        log.Error("Hello!");
                        log.Info("Hello!");
                    }
    11 unions 
        in math this is when you combine 2 sets set are unordered list with unique information
    12 methods
        just a function but inside a class you use then with . notation most of the time.
    13 access controls 
        this is public of private stuff ill and more later but this include static,public,private,pbr,pbv 
    14 constructor/destructor
        constructors
                a constructors is a method that automaticly runs every time we instantiate/make new instance of a class
                does not have a return type and must be the same name as the class

                if you dont write a constructer you get the default constructer which is made for you but that one is empty

                in cpp you must initialize your primitive data types 

                you can make as many as you like ofc with a different amount of arguments

                classname::classname(arg1,arg2)val1(arg1) ,val2(arg2) {} <- this is list initialization
                
                
                #include <iostream>
                example
                    example 1 constructers 
                        class Entity
                        {
                            public:
                                float x,y;

                            Entity(){
                                x = 0.0f;
                                y = 0.0f;
                            }

                            Entity(float i, float i2){
                                x = i; 
                                y = i2; 
                            }

                            void Print()
                            {
                                std::cout << x << ", " << y << std::endl;
                            }
                        }; 

                        int main() 
                        {
                            Entity e;<-calls constructer with no arguments
                            Entity e(10.0f,5.0f);<- calls constructer with 2 arguments
                            e.Print();
                        }
                example 2 
                    class Log{
                        private:
                            Log() {} <- one way 

                        public:
                            Log() = delete;<-second way

                            static void Write(){}
                    }

                    int main(){
                        Log::Write();
                        Log l;<- stop people from doing this
                    }
                    
                    notes
                        first way makes the constructer private so the default does not get made and instance cant get to private
                        
                        second way just delete the constructer cant use what you dont have   
        destructors 
                a method that runs when you destory an object 
                you know something is a destructer when it has a ~ 

                if you use stuff on the heap and need to free that manualy 
                example
                    example 1 
                        class Entity{
                            public:
                                float X, Y;

                                Entity(float x,float y)
                                {
                                    X = x;
                                    Y = y; 
                                }

                                ~Entity()<-this happens when you level the scope of where this was made
                                {
                                    std::cout << "Destroyed" << std::endl;
                                }
                        };
    15 pointers:
        pointers are meant for managing and manipulating memory 
        ------a pointer is a int (a number) that stores a memory adderss that is all it is.
        
        this is memory
        #######################################
        every one of these has an adderss it is 1 byte of data 

        a pointer will tell us where that adderss is in the memory 


        raw pointers 
            void* pointer = 0; <- this is how you get a null pointer you can also replace 0 with NULL
                                    or you can use keyword nullptr 

                explain:
                    the pointer above is completely useless it has a type void witch is nothing 
                    and set to 0 <- 0 is #define NULL in c++ 0 means nothing is there 

                    void* <- the * after the type is how you know it is a pointer 

                    the 3 ways of getting a null pointer 
                        void* ptr = 0;
                        void* ptr = NULL;
                        void* ptr = nullptr;

            getting the adderss of a variable
                int var = 8;
                void* ptr = &var;<- this well set the pointer to the adderss at var
                            &<- this is the adderss of operator or reference symbol
                            *<- dereference operator value at the address so now its open to be 
                                filled

                a pointer still does have a location but when you look at the value of that location it 
                gives you the value of a different place in memory when the pointer is set to  

                so lets say ptr = 0x00A0FB64 this is hexidecimal base 16
                if we were to look this up int memory we word find 08 00 00 00 = to 8 a int takes 
                4 bytes of data so we have for byte with a value of 8 

                if you were to change the type of the point it really does not do anything 

                int* ptr = &var;<- is the same thing but void is safest 
                but void* is still an int becuase pointer are ints that store memory addersses 

            dereferening 
                so say you make a pointer
                int var = 8;
                void* ptr = &var;
                and now i want it to not point to var anymore i can do 
                *ptr <- this is accessing the memory at the pointer value at its own location 

                now this is where types are a little more importent 
                i cant do *ptr = 10;<- becuase the type is void and the computer does 
                    not know how much memory to allocate for the ptr with a value of 10 

                doing what is above is changing the value at the location of var btw 
                
                *p changes pointed to value

                example
                    int* ptr = &var;
                    *ptr = 4;
                    if i print var it will give me 4 now 

            on the stack
                int var = 8;<- this is on the stack

            on the heap
                char* buffer= new char[8];<-this is getting an array but what its really doing 
                                            it is getting us 8 bytes of space on the heap and giving
                                            a pointer to the begining of that box

                memset(buffer,0,8);<-this is a function that fills in data so its getting the pointer
                                    of the first location that is called buffer in this example
                                    0 is the value we want to put in a those locations and
                                    8 is the number of bytes we want to fill in total 

                to be a good boy you should get rid of the memory you 
                put on the heap by saying 
                delete[] buffer;
        smart pointers   
    16 references
        a way of looking at a existing variable 
        reference an existing variable

        after you initialize a variable to a reference you cant reference anything else with that variable
        you also must initialize there is now null reference that you can set later 
        
        a work around to change reference 
            int a = 5;
            int b = 8; 

            int* ref = &a; <-you make the reference a pointer to a's adderss
            ref = &b; <- to change you then reference b 

        pass by value:
            int function(int value){
                return value; 
            } 
            
            int main(){
                int a{3};
                std::cout << function(a) << std::endl; <-it sends a copy of the variable
            }

        pass by reference: 
            int function(int& value){<-now you are taking the reference of the variable you just got 
                value++; 
                return value; 
            } 
            
            int main(){
                int a{3};
                std::cout << function(a) << std::endl;
            }
            you can also do it this way more 
                int function(int* value){<-this is now taking a pointer
                    (*value)++;<-dereference allows you to update value pointer is pointing to 
                    return value; 
                } 
                
                int main(){
                    int a{3};
                    std::cout << function(&a) << std::endl; <-the difference is here this will allow you to change the value of 
                }
        example
            int a = 5;
            int& ref = a;<- not really a variable works like if it was a
                            you can update the value of a by updating the value of ref 
                            think of it like alias 
            
            ref = 2;

            std::cout << a << std::endl;<- this will output 2 a and ref are inter changeable 

        example 2 Incroment
            void Incroment(int value){
                value++;
            }

            int main(){
                int a = 5;
                Incroment(a);
                std::cout << a << std::endl;
            }

            this will make a copy of a becuase it is passing by value so output is still 5 

            to make changes to the value we need to pass by reference

            void Incroment(int* value){ <- so incroment is now taking a pointer
                value++;
            }

            int main(){
                int a = 5;
                Incroment(&a);<- this is sending the reference or adderss of to the function 
                                this allows it to access the memory location
                std::cout << a << std::endl;
            }
    17 static 
        you should try and make all varibles static
        static in a file
            static has to do with translation units the key word static means this name is private to this file/translation unit
            the linker can not use this variable in other files even if linked 

            if you name 2 variables with the same name without calling static the program will compile but fail at 
            linking stage 
        static in a class 
            static variable in a class will share the same memory with all the other instances of the class samething with 
            methods in the class 
         
        

        example 1 
            works 
                # static.cpp
                    static int s_Variable = 5; <-this variable being static will only let me get this memory when im in this file

                #main.cpp
                    #include <iostream>

                    int s_Variable = 10;

                    int main()
                    {
                        std::cout << s_Variable << std::endl;
                        std::cin.get(); 
                    }           
            fails
                # static.cpp
                    int s_Variable = 5; <-this variable <-i remove the static variable making it fail when linking  
                                        because there are 2 variable with the same name.

                #main.cpp
                    #include <iostream>

                    int s_Variable = 10;

                    int main()
                    {
                        std::cout << s_Variable << std::endl;
                        std::cin.get(); 
                    }
        example 2 extern
            #staic.cpp  
                int s_Variable = 5;

            #main.cpp
                #include <iostream>
                    
                extern int s_Variable;<-extern here means this variable is in a different translation unit go look for it
                                        external linking
                                        if you say this it will cause an error if it can't find the variable 

                int main()
                {
                    std::cout << s_Variable << std::endl;
                }
    
        t
    18 #includelllllll
        #include <iomanip>
            //setpresision, fixed, showpoint, left, right, -> set and forget all cout will be formated like this after

            //this will set a decimal point cut off so no numbers past decimal spot you choose 1 is .nothing
            // counts from the left to right 10003 if decimal spot is 3 100 would output starts at 1 
            cout << setprecision(decimal spot) << 3.88 << endl;

            // the fixed starts counting at the . not before 
            cout << fixed << setprecision(3) << pi << endl;

            // will show point even is the value is 0
            cout << showpoint <<setprecisions() << x << endl;

            //all the numbers will be formated till you unset fixed
            cout << setprecision(2) << fixed;
            cout << pi << endl; 
            cout << x << endl; 
            cout << 19.99 << endl; 
            cout.unsetf(ios::fixed);

            //setw is right alined so 10 spaces then say hello not set and foget 
            cout << setw(10) << "Hello" << 8675309 << endl;

            // left moves spaces to after and right is the other way
            cout << left << setw(10) << "Hello" << 

            
            cout << setpresision(3) << fixed;
            cout << setw(15)<< This is row" << setw(10) << 8.1 << sew(10) << 124.90389 << endl; 
            cout << setw(15)18.0 << setw(10) << 38.839 << setw(10) << endl;
        #include <sstream> string stream

            int a{50}; 
            int b{100};
            int result = a + b 
            string display

            // doing it this way you dont have to manually chagne it yourself
            stringstream ss; 
            ss << int << str << int << result << endl; 
            
            //adds only 1 at a time to display endl; will then give you a newline each time
            while(ss >> display)
                cout << dispaly << endl;
            
            // this will give me the full display all at once
            getline (ss, display)
                cout << display << endl;

            // gets every thing in the string value
            display = ss.str(); 
            cout << display << endl; 

            ss.clear(); no more stuff in the string 

            --------------parsing the string

            string grades = "2,3,3,3,2,2,5,5"; 
            string grade; 
            int totalGrade; 
            int numgrade;

                //give you the whole string 
            ss.str(grades);
                cout << ss.str() << endl;

                //will give you one element at a time 
                while (getline(ss,grade, ','))  // stream , string going into , seperater
                    cout << grade << endl;
                    totalGrade += stoi(grade); 
        #include <cstdlib>
            will give you numbers by default in a range 0 to 32,767

    
            using namespace std;

            rand(); <- give you a random number based on the seed the seed only changes if you change it

            srand(int) <- this will change the seed but doesn't fix the problem all the way because we need to change
                        the int that is being passed

            #include <ctime>
            int x = time(0); <- this will give you time repersented by an int
            srand(x); <- now the seed will be based on the system clock

            to set the bounds just mod the number by the upper bound 
            add to it the lower bound after doing mod 

            in all mod = upper bound 
                    add = lower bound

                example 
                    int x = time(0); <- used for setting the seed with looking at system time
                    srand(x); <- do this to set the seed 
                    cout << rand() << endl; <-this is 0 to 32,727 based on the system you are using
                    cout << 1 + rand() % 6 << endl; <-this will give you the range 1 to 6 
    19 initialisation list 
        a way to initialize members in a constructor and just better and safer 

        examples
                normal 
                    class Entity
                    {
                        private:
                            std::string m_Name;
                        public:
                            Entity()
                            {
                                m_Name = "Unknown"
                            }
                            Entity(const std::string& name)
                            {
                                m_Name = name;
                            }

                            const std::string& GetName() const {return m_Name;}
                    }; 

                initialisation list 
                    class Entity
                    {
                        private:<- the order that the list will run is this not how you put it 
                            std::strign m_Name;
                            int m_Score;
                        public:
                            Entity(): m_Name("Unknown"), m_score(0){} <- down here no matter what 

                            Entity(const std::string& name): m_Name(name){}

                            const std::string& GetName() const {return m_Name;}
                    }; 
                
                how not to use and why its bad
                    class Entity
                    {
                        private:<- the order that the list will run is this not how you put it 
                            std::strign m_Name;
                            int m_Score;
                        public:
                            Entity(): m_score(0){
                                m_Name = std::string("Unknown");<- will construct 2 times
                            } <- down here no matter what 

                            Entity(const std::string& name): m_Name(name){}

                            const std::string& GetName() const {return m_Name;}
                    }; 

        why do it like this 
            1st style just looks better takes up less space also faster 2 will tell you why
            2nd you can accidentally construct members more then once especially when part of a different class     
    20 Arrow Operator
        short cut to dereferencing. dereferencing is go to the end of the pointer and use the information there 
                                        use data at adderss 
        example
            Arrow to dereference 
                #include <iostream>
                #include <string>

                class Entity{
                    public:
                        int x;
                        void Print() const {std::cout<< "Hello!" << std::endl;}
                }; 

                int main()
                {
                    Entity e;
                    e.Print(); 

                    Entity* ptr = &e;<- making a pointer ptr point to e adderss e is instance of Entity
                    Entity& entity = *ptr; <-dereferencing that pointer no longer point to e use data at e
                    entity.Print() <- call print method on instance of Entity class

                    or 

                    Entity* ptr = &e; <- same thing as above
                    (*ptr).Print(); <- dereference then method print one line need the ()

                    or 
                    
                    ptr->Print(); <- dereference pointer then call method print()

                    ptr-> x = 2; 

                }

            how to overload ->
                                #include <iostream>
                #include <string>

                class Entity{
                    public:
                        int x;
                        void Print() const {std::cout<< "Hello!" << std::endl;}
                }; 

                class ScopedPtr{<- this is a smart pointer 
                    private:
                        Entity* m_Obj; <-raw pointer 
                    public:
                        ScopedPtr(Entity* entity): m_Obj(entity){} <-stores the raw pointer in m_Obj no allocation happens 
                                                                    just saves the address

                    ~ScopedPtr()<- destructor frees heap memory
                    {
                        delete m_Obj;
                    }

                    Entitiy* operator->() 
                    {
                        return m_Obj;
                    }
                    
                    or for const

                    const Entitiy* operator->() const
                    {
                        return m_Obj;
                    }
                };


                int main()
                {
                    ScopedPtr entity = new Entity();
                    const ScopedPtr entity = new Entity();
                    entity->Print();
                }

            to get off set
                struct Vector3
                {
                    float x,y,z;
                };

                int main()
                {
                    int offset = (int)&((vector3*)0)->x; <- how you find how far one piece of memory is from the other 
                    or 
                    int offset = (int)&((vector3*)nullptr)->x;

                }
    21 New keyword
        new is just an opperator so you can overload it
        new is how you allocate memory on the heap and also call the constructor
        new finds a group of memory big enough for our need from a thing called free list which is a list of free memory
            then gives us a pointer to that block of memory
        
        if you use new MUST USE delete pointer; c++ version of free() and delete calles desstructor
        delete is also a opperator no one is safe 

        new vs malloc 

            new will call the constructor gives back addresss of pointer to that memory

            malloc will purely allocate the memory and give us a pointer to that memory

        example
            what the code looks like
                int main()
                {
                    int a = 2; <-on the stack copy of 2 set to a
                    int* b = new int; <- pointer to address of 4 bytes of memory on the heap 

                    int* b = new int[50];<- for an array will give me the start 

                    Entity* e = new Entity[50];<- array of entitys <- delete[] for this one 

                    Entity* e = (Entity*)malloc(sizeof(Entity));<- new calls this
                                 malloc(sizeof(Entity)) <- c verison 

                    Entity* e = new(location) Entity(); <- if you know the location 
                }
    22 operator overloading 
        operator is a symbol we use to just do a function * -> & | new delete , () and so much more 
        overloading just means giving a meaning to or changing the meaning 
        should not use this that much if 
    
        example
            way1

                struct Vector2 {
                    float x,y;
                    
                    Vector2(float x,float y): x(x), y(y) {}

                    Vector2 Add(const Vector2& other) const
                    {
                        return Vector2(x + other.x, y + other.y); <- not overloading to avoid overloading 
                                                                    but is what it does
                    }

                    Vector2 Multiply(const Vector2& other) const{
                        return Vector2(x * other.x,y * other.y);
                    }

                    Vector2 operator+(const Vector2& other) const{
                        return Add(other);
                    }

                    Vector2 operator*(const Vector2& other) const {
                        return Multiply(other);
                    }
                }; 

                int main()
                {
                    Vector2 position(4.0f, 4.0f);
                    Vector2 speed(0.5f,1.5f);
                    VEctor2 powerup(1.1f,1.1f);
                    
                    Vector2 result = position.Add(speed); <- what you do to not do overloading / same function tho

                    Vector2 result = position.Add(speed.Multiply(powerup)); <- see how this is getting hard to read 
                                        these do the same thing 
                    Vector2 result = position + speed * powerup;<- now this just calls a function a passes the 2 instances being 
                                                                    + or * 
                }
            way2 this is better you can also only do operator ones 
                
                struct Vector2 {
                    float x,y;
                    
                    Vector2(float x,float y): x(x), y(y) {}

                    Vector2 Add(const Vector2& other) const
                    {
                        return operator+(other);<- you can treat this like a function
                    }

                    Vector2 Multiply(const Vector2& other) const{
                        return *this + other; <- this is the 3rd way current instance + other
                    }

                    Vector2 operator+(const Vector2& other) const{
                        return Vector2(x + other.x, y + other.y);
                    }

                    Vector2 operator*(const Vector2& other) const {
                        return Vector2(x * other.x,y * other.y);
                    }

                    bool operator==(const Vector2 other) const
                    {
                        return x==other.x && y == other.y;
                    }

                     bool operator!=(const Vector2 other) const
                    {
                        return !(*this==other); <- better 
                        or 
                        return !operator ==(other);
                    }
                }; 

                std::ostream& operator<<(std::ostream& stream, const Vector2& other)
                {
                    stream << other.x << ", " << other.y; <- like this 
                    return stream;
                }

                int main()
                {
                    Vector2 position(4.0f, 4.0f);
                    Vector2 speed(0.5f,1.5f);
                    VEctor2 powerup(1.1f,1.1f);

                    Vector2 result = position + speed * powerup;

                    std::cout << result2 << std::endl; <- will output x, y 

                    if (result == result) <- this will now work 
                }
    23 virtual Functions
        virtual functions allow us to overide methods in subclasses
        

        example
            example 1
                #include <iostream>
                #include <string>

                class Entity{
                    public:
                        std::string GetName(){return "Entitity";}
                };

                class Player : public Entity{
                    private:
                        std::string m_Name;
                    public:
                        Player(const std::string& name):m_Name(name) {}

                        std::string GetName(){return m_Name;}
                }; 

                int main()
                {
                    Entity* e = new Entity();<- entity* is a pointer of size Entity
                    std::cout << p->GetName() << std::endl;

                    Player* p = new Player("name");
                    std::cout << p->GetName() << std::endl;

                    Entity* entity = p; <- you will get an entity part of the player p you can only use entity parts tho but the whole
                                            player is there you just cant use it
                    std::cout << entity->GetName() << std::endl;
                }
            
            example 2
                    #include <iostream>
                    #include <string>

                    class Entity{
                        public:
                            std::string GetName(){return "Entitity";}
                    };

                    class Player : public Entity{
                        private:
                            std::string m_Name;
                        public:
                            Player(const std::string& name):m_Name(name) {}

                            std::string GetName(){return m_Name;}
                    }; 

                    void PrintName(Entity* entity)
                    {
                        std::count << entity->GetName() << std::endl;
                    }

                    int main()
                    {   
                        Entity* e = new Entity();
                        PrintName(e)

                        Player* p = new Player("cherno");
                        PrintName(p);  
                    }



